export enum tagTypes {
    user = 'user',
    admin = 'admin',
    customer = 'customer',
    vendor = 'vendor',
    products = 'products',
    carts = 'carts',
    review = 'review',
    payment = 'payment',
    orders='orders',
    shops='shops',
    categories='categories'
 }
 
 export const tagTypesList = [
    tagTypes.user,
    tagTypes.admin,
    tagTypes.shops,
    tagTypes.customer,
    tagTypes.vendor,
    tagTypes.products,
    tagTypes.carts,
    tagTypes.review,
    tagTypes.orders,
    tagTypes.payment,
    tagTypes.categories
 ];